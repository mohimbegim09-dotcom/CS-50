names = []

for _ in range(3):
    name = input('What is your name?')
    names.append(name)

    #OR

for _ in range(3):
    names.append(input('What is your name?'))


    # and if you want to sort these names in alphabetic order

for name in sorted(names):
    print(f'hello, {name}')
    